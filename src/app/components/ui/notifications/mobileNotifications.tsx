'use client'
import React from 'react'

type Props = {}

const MobileNotifications = (props: Props) => {
  return (
    <div>MobileNotifications
        {/* If opened, set viewed for this user to true. If alll viewed, removed red notification circle. */}
    </div>
  )
}

export default MobileNotifications