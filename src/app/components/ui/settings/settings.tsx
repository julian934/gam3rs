'use client'
import React from 'react'

type Props = {}
//Need template in place for data.
const Settings = (props: Props) => {
  return (
    <div className='text-red bg-green ' >Settings</div>
  )
}

export default Settings